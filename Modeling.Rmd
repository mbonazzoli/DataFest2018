---
title: "Modeling"
author: "Brett Graham"
date: "3/23/2018"
output: html_document
---

#Linear Model
Dependent:
Total clicks

Independent:
Number of job postings
Number of job-seekers
Indeed exposure
Internet Access


```{r}
clickModelBasic <- lm(cityClicks ~ 0 + jobPostings,data= indeedCity)
summary(clickModelBasic)

```

```{r}
clickModel <- lm(cityClicks ~jobPostings + unemployedPop + jobsPerUnemployed,data= indeedCity)
clickResid <- resid(clickModel)
summary(clickModel)
# clickModel$residuals
resids <- as.data.frame(clickModel$residuals)
fit <- as.data.frame(clickModel$fitted.values)

model <- cbind(fit,resids)
colnames(model) <- c("Prediction","Residual")

indeedCity <- indeedCity %>%
  bind_cols(model)


```












Dependent:
Average clicks per posting

Independent:
Days of ad on site
Number of job posting


Dependent:
Average applies per posting

Independent:

